import * as React from "react";

class DeviceDetail extends React.Component{
    state: {
        device: { id: '', name: ''}
    }

    componentDidMount() {
    }

    render() {
        return (
            <React.Fragment>

            </React.Fragment>
        );
    }
}

export default DeviceDetail;